# Alows me to enter the inputs of the variables as well as adding instrustions.
B=float(input("input B then press Enter: "))

A=float(input("Input A then press Enter: "))

C=float(input("input C then press Enter: "))

# Maths up the variables and shows the anwser
print(B**2-4*A*C)